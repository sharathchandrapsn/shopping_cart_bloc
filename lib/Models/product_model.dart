class ProductModel {
  final String? itemId;
  final String? name;
  final int? quantity;
  final double? price;
  final String? imageUrl;

  ProductModel(
      {this.itemId, this.name, this.quantity, this.price, this.imageUrl});
}
